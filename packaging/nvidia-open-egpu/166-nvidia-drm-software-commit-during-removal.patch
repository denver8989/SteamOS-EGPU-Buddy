--- a/kernel-open/nvidia-drm/nvidia-drm-modeset.c
+++ b/kernel-open/nvidia-drm/nvidia-drm-modeset.c
@@ -405,6 +405,14 @@
     struct drm_crtc_state *old_crtc_state, *new_crtc_state;
     int i;
     int ret;
+    /*
+     * Device being removed (eGPU surprise unplug): no hardware behind this
+     * device any more.  Let the commit go through as a software-only state
+     * swap so the DRM core can tear down planes/framebuffers (rmfb on close),
+     * which drops the GEM/dma-buf references that otherwise pin this module.
+     */
+    const bool removing = READ_ONCE(nv_dev->inRemoval) ||
+                          READ_ONCE(nv_dev->inSurpriseRemoval);
 
     /*
      * If sub-owner permission was granted to another NVKMS client, disallow
@@ -457,7 +465,7 @@
              * If flip event will be generated by hardware
              * then defer flip object processing to flip event from hardware.
              */
-            if (__will_generate_flip_event(crtc, old_crtc_state)) {
+            if (!removing && __will_generate_flip_event(crtc, old_crtc_state)) {
                 if (nv_drm_vblank_module_param && nv_new_crtc_state->nv_flip->event != NULL) {
                     drm_crtc_vblank_get(crtc);
                 }
@@ -468,13 +476,11 @@
                 nv_new_crtc_state->nv_flip = NULL;
             }
 
-            ret = __nv_drm_convert_in_fences(nv_dev,
-                                             state,
-                                             crtc,
-                                             new_crtc_state);
-
-            if (ret != 0) {
-                return ret;
+            if (!removing) {
+                ret = __nv_drm_convert_in_fences(nv_dev, state, crtc, new_crtc_state);
+                if (ret != 0) {
+                    return ret;
+                }
             }
         }
 
@@ -496,7 +502,8 @@
          nv_drm_write_combine_flush();
     }
 
-    if (!nvKms->applyModeSetConfig(nv_dev->pDevice,
+    if (!removing &&
+        !nvKms->applyModeSetConfig(nv_dev->pDevice,
                                    requested_config,
                                    &reply_config,
                                    commit)) {
@@ -513,7 +520,7 @@
         }
     }
 
-    if (commit && nv_dev->supportsSyncpts) {
+    if (commit && !removing && nv_dev->supportsSyncpts) {
         /* commit is true so we check old state */
         for_each_old_crtc_in_state(state, crtc, old_crtc_state, i) {
             /*! loop over affected crtcs and get NvKmsKapiModeSetReplyConfig */
@@ -757,7 +764,8 @@
          * If the legacy LUT needs to be updated, ensure that the previous LUT
          * update is complete first.
          */
-        if (crtc_state->color_mgmt_changed) {
+        if (crtc_state->color_mgmt_changed &&
+            !READ_ONCE(nv_dev->inRemoval) && !READ_ONCE(nv_dev->inSurpriseRemoval)) {
             NvBool complete = nvKms->checkLutNotifier(nv_dev->pDevice,
                                                       nv_crtc->head,
                                                       !nonblock /* waitForCompletion */);
